<clickhouse>
    <storage_configuration>
        <disks>
            <!-- Define the default disk explicitly for compatibility with old tables. -->
            <default>
                <type>local</type>
                <path>/var/lib/clickhouse/data/</path>
            </default>

            <!-- A local disk for hot data, limited to 1GB. -->
            <local_data_disk>
                <type>local</type>
                <path>/var/lib/clickhouse/data/</path>
                <max_data_part_size>1073741824</max_data_part_size>
            </local_data_disk>

            <!-- S3 disk for cold data. -->
            <s3_disk>
                <type>s3</type>
                <endpoint>https://nbg1.your-objectstorage.com/rybbit/clickhouse/</endpoint>
                <access_key_id from_env="S3_ACCESS_KEY"/>
                <secret_access_key from_env="S3_SECRET_KEY"/>
                <region from_env="S3_REGION"/>
                <use_virtual_addressing>false</use_virtual_addressing>
                <metadata_path>/var/lib/clickhouse/disks/s3_disk_metadata/</metadata_path>
            </s3_disk>
        </disks>

        <policies>
            <!-- This policy defines a tiered storage setup. -->
            <tiered_s3_policy>
                <volumes>
                    <!-- Use the explicit 'default' disk for the migration volume. -->
                    <default>
                        <disk>default</disk>
                    </default>
                    <!-- Hot data stays on the local disk. -->
                    <hot>
                        <disk>local_data_disk</disk>
                    </hot>
                    <!-- Cold data moves to S3. -->
                    <cold>
                        <disk>s3_disk</disk>
                    </cold>
                </volumes>
                <move_factor>0.1</move_factor>
            </tiered_s3_policy>
        </policies>

        <profiles>
            <default>
                <default_storage_policy>tiered_s3_policy</default_storage_policy>
            </default>
        </profiles>
    </storage_configuration>
</clickhouse>